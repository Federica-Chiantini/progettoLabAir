import { FiltraPerNomePipe } from './filtra-per-nome.pipe';

describe('FiltraPerNomePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltraPerNomePipe();
    expect(pipe).toBeTruthy();
  });
});
